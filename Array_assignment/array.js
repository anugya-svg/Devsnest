function is_array(x){
    return Array.isArray(x);
}

function array_clone(x){
    var p=x;
    return p;
}
function first(x,n=1){
let a;
    if(n<0)
    {
        a=x.length;
    }
    else a=0;
    return x.slice(a,n);
}
function merge(x){
    return x.join(',');
}

function mostFreq(x){
    for(let freq of x)
    {
        
    }
}
